#include <stdio.h>

struct intercept {
   int x;
   int y;
};

int main() {
   
   // User input points
   std::array<Point, 3> array = { { 0, 0 }, { 0, 1 }, { 1, 1 } };
   
   // Line formula: y = mx + b
   M = 1;
   B = 1;
   
   // Radius r
   R = 1;
   
   // Calculate intercept points for line and circles
   
   
   
   // List intercept points in hash table
   
   
   // Sort intercept points (Quick Sort)
   
   
   
   // Sweep hash table
   
   
   
   
}
